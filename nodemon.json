{
    "watch": ["server.ts"],
    "delay": "1000",
    "env": {
        "NODE_ENV": "development"
    },
    "exec": "ts-node --project ./tsconfig.server.json -r tsconfig-paths/register ./server.ts",
    "ext": "ts"
}